import type { Metadata } from "next";

export const metadata: Metadata = { title: "Kayıt Ol — Ücretsiz Başla" };

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
